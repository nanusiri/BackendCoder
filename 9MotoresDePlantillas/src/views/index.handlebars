<!DOCTYPE html>
<html lang="en">

<head>
    <meta charset="UTF-8">
    <meta name="viewport" content="width=device-width, initial-scale=1.0">
    <title>Document</title>
</head>

<body>
    <h1>Hola {{user.name}}</h1>
    {{#if isAdmin}}
    {{#each food}}

    <div>
        <p>{{this.name}}</p>
        <p>{{this.price}}</p>
    </div>

    {{/each}}
    {{else}}
    <p>No eres Admin</p>
    {{/if}}

    <script src="/9MotoresDePlantillas/src/public/js/index.js"></script><!--AGREGO JS-->
</body>

</html>